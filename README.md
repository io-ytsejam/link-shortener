Project consists of 3 parts:
 - backend
 - frontend
 - example
 
How to run it?
1. Run `go run main` from `backend` catalog (default port is 8080)
2. Build frontend by running `yarn build` from frontend `catalog`
3. App should be available under http://localhost:8080 or in form of widget in `example/index.html`
